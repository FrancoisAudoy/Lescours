%{
 #include <stdio.h>

  %}

%start A
%%
A : S {;}
S : 'a'S'b' {;}
| 'a''b' {;}
;

%%

int yylex(){
  return getchar();
}
int yyerror(char *s){
  fprintf(stderr, "*** ERROR %s\n",s);
  return 0;
}

int main(int argc, char **argv){
  yyparse();
  return 0;
}
